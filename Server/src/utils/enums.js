const loginType = {
    EMAIL: 'email',
    REFRESH: 'refresh',
}

const userType = {
    ADMIN: 'admin',
    USER: 'user',
}

const fileType = {
    FOLDER: 'folder',
    FILE: 'file',
}

module.exports = {
    loginType,
    userType,
    fileType,
}